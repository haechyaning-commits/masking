# 작업 기록 (WORKLOG)

## 2026-07-29

### 오늘 한 것

세 가지 요청을 구현·검증하고 브랜치에 반영했습니다.

1. **마스킹 완료 파일에서 텍스트 추출 (#1)**
   - 결과 PDF에 **보이지 않는(추출용) 텍스트층**을 추가. 이미지 위에 텍스트를 얹어, 마스킹된 파일을 드래그·복사·검색하면 본문 텍스트가 나오도록 함.
   - **마스킹된 개인정보는 마스킹 문자로 대체**되어 추출되지 않음(원문 유출 없음). 마스킹되지 않은 본문만 원문으로 추출됨.
   - 한글 지원을 위해 **서브셋 Noto Sans KR 폰트 + fontkit**을 독립 실행본(`masking-standalone.html`)에 내장. 오프라인 동작 유지.
   - claude.ai 아티팩트본(`masking-artifact.html`)은 용량을 위해 폰트 미포함(기존처럼 이미지 전용) → 게시 용량 제한 회피.
   - 폰트에 없는 글자(한자·이모지 등)는 공백으로 안전 치환(추출 깨짐 방지).

2. **여러 마스킹 문자 혼합 적용 (#2)**
   - 마스킹 문자 선택기를 **다중 선택** 가능하게 변경(최소 1개 유지).
   - 출력 시 선택한 문자들을 각 자리에 **무작위로 섞어** 실제 마스킹 문서처럼 표기(예: `*`+`○` → `*○**○…`).

3. **수동 마스킹 "문자" 방식 버그 수정 (#3)**
   - 기본값이 `문자`로 선택돼 있어도 내부 상태가 `box`로 남아 검은박스가 생성되던 문제 수정.
   - 시작 시 실제 선택된 라디오값과 상태를 동기화.

### 검증 (Chromium/Playwright + pdf.js 추출)

- 샘플 PDF 3쪽으로 전체 흐름 통과.
- #1: 한글 본문 추출 정상, 개인정보는 마스킹 문자로 표기(주민번호 뒤 7자리·전화 뒤 4자리·이메일·계좌 마스킹 확인), 글리프 누락(NUL) 없음.
- #2: `*`+`○` 동시 선택 시 출력에 두 문자 혼합 확인(`*`=61, `○`=44).
- #3: 수동 모드 기본 방식으로 마스킹 시 `symbol`(문자) 스타일 적용 확인(검은박스 아님).

### 파일 크기

- `masking-standalone.html`: 약 2.0MB → **6.6MB** (한글 폰트 내장 때문). 로컬 실사용본만 해당.
- `masking-artifact.html`: 약 2.0MB 유지.

### 변경 파일

- `web/app-artifact.js` — 텍스트층 생성, 다중 마스킹 문자, 수동 스타일 동기화
- `web/body.html` — "여러 개 선택 가능" 안내, 출력 안내 문구 id
- `web/build.js` — 독립 실행본에만 폰트/fontkit 내장하도록 빌드 분기
- `vendor/fontkit.umd.js`, `vendor/NotoSansKR-subset.ttf` — 신규 에셋
- `web/masking-standalone.html`, `web/masking-artifact.html` — 재빌드
- `README.md` — 출력/보안/로드맵 갱신

---

### 내일 할 것

- [ ] **폰트 용량 최적화 검토** — 현재 서브셋 2.9MB(→내장 시 6.6MB). 자주 쓰는 한글 위주로 더 줄일지, 한자(하나+) 포함 여부 결정.
- [ ] **실제 감사 문서로 정확도 검증** — 텍스트층의 위치·순서, 마스킹 범위(부분/전체) 정확도 육안 확인.
- [ ] **영역 드래그 마스킹의 텍스트층 처리** 정밀화 — 현재는 겹치는 조각 전체를 마스킹(보수적). 글자 단위로 정밀 매칭할지 검토.
- [ ] **이름·기관 자동 탐지(NER)** 개선 — 기본 상태에서 사람 이름이 텍스트층에 노출됨(현재는 사용자가 단어 입력/클릭해야 마스킹). 자동화 검토.
- [ ] **React/Vue 이관 설계** — 텍스트층 로직을 모듈로 분리, 폰트는 지연 로딩 에셋으로. 단일 파일 용량 문제 해소.
- [ ] (선택) 미리보기에서 "텍스트 추출 가능" 여부를 사용자에게 명시적으로 안내.
