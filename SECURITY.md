# 보안 설명서 — 데이터를 어떻게 지키는가

이 도구는 개인정보(주민등록번호·전화·이메일·계좌·이름·기관 등)를 다루므로, "업로드한 파일이 어디로도 새지 않는다"는 것이 핵심입니다. 아래는 그 보장 방식과 **누구나 직접 확인하는 방법**입니다.

## 1. 무엇이 보장되는가

| 항목 | 내용 | 검증 결과 |
|---|---|---|
| **서버 미전송** | 파일 읽기·탐지·마스킹·저장이 모두 브라우저 안에서만 실행. 어떤 서버로도 파일이 전송되지 않음 | 전체 처리 중 **외부 네트워크 요청 0건** |
| **네트워크 호출 없음** | 앱 코드에 `fetch`/`XMLHttpRequest`/`WebSocket`/`sendBeacon` 등 전송 코드가 전혀 없음 | 앱 코드 grep 결과 **0건** |
| **외부 의존 없음** | pdf.js·pdf-lib를 파일 내부에 포함(CDN 참조 없음). 인터넷 없이 동작 | 외부 URL 참조 **0건** |
| **진짜 삭제(true redaction)** | 결과는 페이지 이미지화 → 마스킹된 원본 글자가 결과 PDF에서 추출·복사 불가 | 결과 PDF **추출 텍스트 0자** |
| **메타데이터 제거** | 원본 작성자·제목·주제·키워드 등 문서 메타데이터를 비움 | title·author·subject **비어 있음** |

> 라이브러리 서명(Producer=pdf-lib)만 남으며 이는 개인정보가 아닙니다.

## 2. 직접 확인하는 방법 (권장)

보안은 "믿어달라"가 아니라 **확인 가능해야** 합니다.

1. **인터넷을 끄고 사용해 보세요.** Wi-Fi/랜을 끊은 상태에서 `web/masking-standalone.html`을 열어 마스킹까지 정상 동작하면, 데이터가 밖으로 나갈 수 없다는 뜻입니다.
2. **개발자도구 → Network 탭**을 열고 파일을 처리해 보세요. 파일 관련 외부 요청이 **하나도 없음**을 볼 수 있습니다.
3. **결과 PDF에서 마스킹 부분을 드래그·복사**해 보세요. 아무 것도 선택/복사되지 않습니다(이미지라서).
4. **소스는 공개**되어 있습니다(이 저장소). `web/app-artifact.js`에 네트워크 전송 코드가 없음을 직접 확인할 수 있습니다.

## 3. 남는 리스크 / 권장 사용법

- **호스팅본(claude.ai 아티팩트)** 은 편의를 위한 것이며, 페이지 자체는 claude.ai에서 로드됩니다(처리는 여전히 브라우저 내부). **실제 민감한 데이터**는 내려받은 **`web/masking-standalone.html`(로컬 더블클릭)** 로 처리하는 것을 권장합니다. 이 경우 어떤 서버도 개입하지 않습니다.
- 이 도구는 **1차 필터링**입니다. 자동 탐지는 오탐·누락이 있을 수 있으므로, 결과물을 **반드시 육안으로 최종 확인**한 뒤 업로드하세요.
- 사내 배포 시, `web/masking-standalone.html` 파일 하나만 공유하면 됩니다(추가 설치·서버 불필요).

## 4. 요약

> 업로드한 PDF와 마스킹 결과는 **브라우저 내에서만** 처리되며, 외부로 전송·저장되지 않습니다. 결과물은 마스킹된 원본 글자가 남지 않는 이미지 PDF이며, 원본 메타데이터도 제거됩니다.
