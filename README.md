# 공공감사포털 업로드 전 개인정보 마스킹 도구

감사데이터를 공공감사포털에 올리기 전, 문서의 개인정보를 **자동 탐지 → 사람 검토 → 마스킹 PDF 저장**하는 **브라우저 단독 실행 도구**입니다. 설치·서버·인터넷 없이 동작하며, 파일이 외부로 전송되지 않습니다.

> **1차 필터링 도구**입니다. 자동 탐지는 오탐·누락이 있을 수 있으니 결과물을 **반드시 육안으로 최종 확인**한 뒤 업로드하세요.

## 바로 쓰기

- **`web/masking-standalone.html`** 파일을 내려받아 **더블클릭** → 브라우저에서 바로 실행 (권장, 완전 오프라인·다운로드 정상).
- 사용 흐름: PDF 업로드 → 탐지 결과 검토 → (필요 시) 이름·기관 입력·직접 지정 → 마스킹 수준 선택 → **마스킹 PDF 생성/다운로드**.
- 시험용 샘플: `samples/복무감사_결과보고서_샘플.pdf` (모든 개인정보가 가공된 허구 데이터).

## 기능

### 1) 번호류 자동 탐지 (정규식)
주민등록번호·전화번호·이메일·계좌번호를 자동 탐지. 공백·점·전각숫자 등 표기 변형에도 대응.

### 2) 이름·기관/부서 (기호 치환)
형식이 없어 자동탐지가 어려운 이름·기관은 두 가지로 처리:
- **가릴 단어 목록 입력** — 입력한 이름/기관을 문서 전체에서 찾아 치환
- **접미어 규칙 자동** — 센터·사무소·구청·주식회사·병원 등으로 끝나는 이름의 앞부분 자동 탐지 (접미어는 유지: `서울복지센터` → `○○○○센터`)

### 3) 직접 지정 (클릭 / 드래그)
- 자동이 놓친 단어를 **클릭**하면 마스킹. **"같은 단어 모두 가리기"** 로 문서 전체 일괄 처리.
- 표·도장 등 비텍스트 영역은 **드래그**로 지정. 추가 항목은 다시 **클릭하면 삭제**.

### 4) 마스킹 수준 — 부분 / 전체 선택
**부분 마스킹(표준·권장)** 은 국내 공공기관 실무 기준을 따릅니다:

| 항목 | 가리는 부분 | 예 |
|---|---|---|
| 주민등록번호 | 뒤 7자리 | `990101-*******` |
| 전화·휴대폰 | 뒤 4자리 | `010-1234-****` |
| 이메일 | 아이디 앞 3자만 남김 | `abc***@sr.co.kr` |
| 계좌번호 | 앞 6자리만 남김 | `110-234-******` |
| 이름 | 가운데 글자 | `홍*동` |
| 기관·부서 | 접미어 앞 전부 | `○○○○실` |

**전체 마스킹** 은 값 전체를 가림(번호=검은박스, 이름·기관=마스킹 문자). 마스킹 문자(`*` 기본, ○·△·□·♠ 선택)는 바꿀 수 있습니다.

### 5) 출력
결과는 **페이지 이미지화**로 저장되어 마스킹된 원본 글자가 추출되지 않으며, 원본 PDF 메타데이터도 제거됩니다.

**추출용 텍스트층**(독립 실행본 `masking-standalone.html` 전용): 이미지 위에 보이지 않는 텍스트층을 얹어, **마스킹 완료 파일에서도 본문을 텍스트로 추출·검색**할 수 있습니다. 이때 **마스킹된 개인정보는 마스킹 문자로 대체**되어 추출되지 않습니다(원문 유출 없음). 한글 지원을 위해 서브셋 Noto Sans KR 폰트를 내장하므로 독립 실행본 파일 크기가 커집니다(≈6.6MB). claude.ai 아티팩트용 `masking-artifact.html`은 용량을 위해 폰트를 넣지 않아 텍스트층 없이 이미지로만 저장됩니다.

## 보안 (데이터를 어떻게 지키는가)

- **완전 클라이언트 사이드**: 파일은 브라우저 메모리에서만 처리됩니다. 코드에 업로드/네트워크 전송(fetch·XHR 등)이 **전혀 없습니다**.
- **외부 의존 0**: pdf.js·pdf-lib를 파일 내부에 포함(CDN 없음) → **인터넷을 끊어도 동작**합니다(이것이 곧 "데이터가 밖으로 나가지 않는다"는 증거).
- **진짜 삭제**: 출력이 이미지화라 마스킹 부분의 원본 텍스트가 남지 않습니다. (독립 실행본의 추출용 텍스트층에도 마스킹된 개인정보는 마스킹 문자로만 들어가며 원문은 포함되지 않습니다.)
- **권장**: 실데이터는 호스팅본이 아니라 **`web/masking-standalone.html`(로컬 더블클릭)** 로 처리하세요. 서버가 개입하지 않습니다.

## 지원 범위 / 한계

- **지원**: 텍스트 레이어가 있는 PDF.
- **미지원**: 스캔본(이미지) PDF(OCR 필요), HWP(별도 PDF 변환 선행).
- **한계**: 사람 이름 등 비정형 정보는 완전 자동이 아니며(직접 입력/클릭 필요), 정규식 특성상 오탐·누락 가능. 좌표는 근사이며 세로쓰기·회전 페이지는 정확도가 떨어질 수 있음.

## 파일 구조

```
web/                     단일 파일(아티팩트) 앱 — 현재 버전
  masking-standalone.html  더블클릭 실행용 (배포본)
  masking-artifact.html    claude.ai 아티팩트용(body-only)
  body.html · masking.css · app-artifact.js   소스
  build.js                 위 소스 + vendor/js 를 단일 파일로 조립
js/detectors.js          개인정보·이름/기관 탐지 로직 (공용)
vendor/                  pdf.js · pdf-lib (오프라인용 포함)
samples/                 시험용 샘플 PDF (허구 데이터)
index.html · js/app.js · css/styles.css   초기 프로토타입(참고)
```

### 빌드

소스를 고친 뒤 단일 파일을 다시 만들려면:

```
cd web && node build.js
```

## 로드맵

- [x] PDF 정형 개인정보 탐지 + 검토 + 마스킹
- [x] 표준 부분/전체 마스킹 선택
- [x] 이름·기관 기호 치환 + 직접 지정(클릭/드래그)
- [x] 여러 마스킹 문자 혼합 적용 (`*`+`○` 등 동시 선택)
- [x] 마스킹 완료 파일의 본문 텍스트 추출/검색 (독립 실행본, 한글 폰트 내장)
- [ ] 실제 감사 문서로 정확도 검증 및 규칙 보정
- [ ] 이름 NER 자동탐지 / HWP 지원 재검토

## 개인정보 처리 안내

업로드한 PDF와 마스킹 결과는 **브라우저 내에서만 처리**되며 외부 서버로 전송·저장되지 않습니다.
